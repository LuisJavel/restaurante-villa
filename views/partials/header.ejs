<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Le Château - Restaurante Gourmet</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="logo">Le Château</a>
            <button class="menu-toggle" aria-label="Menú">
                <span></span><span></span><span></span>
            </button>
            <ul class="nav-menu">
                <li><a href="/" class="<%= pagina === 'inicio' ? 'active' : '' %>">Inicio</a></li>
                <li><a href="/menu" class="<%= pagina === 'menu' ? 'active' : '' %>">Menú</a></li>
                <li><a href="/reservas" class="<%= pagina === 'reservas' ? 'active' : '' %>">Reservas</a></li>
                <li><a href="/galeria" class="<%= pagina === 'galeria' ? 'active' : '' %>">Galería</a></li>
                <li><a href="/contacto" class="<%= pagina === 'contacto' ? 'active' : '' %>">Contacto</a></li>
            </ul>
        </div>
    </nav>

    <main>